# Lock & Scroll — data model

Field map for the two record types (**key** and **vehicle**) plus every
satellite store that hangs off them. Generated by reading `index.html`
against the live seed data (515 `DB` records, 41 `customKeys`, 1847 vehicle
application entries, 28 `customVehicles`, 168 `lishiVehicleData` groups).

Counts in the tables are how many records actually carry a non-empty value —
useful for judging which fields an importer must fill vs. which are rare.

---

> ### Where the data actually lives (v245 onward)
>
> `index.html` ships **reference data only**: the 475-record seed catalog,
> `lishiData`, `fidPrefixes`, `DEFAULT_VENDORS` and the enum tables. Every
> store holding *your* data — `customKeys`, `vendorPrices`, `fidAssignments`,
> `keyRelationships`, `fccRelationships`, `keyInfoOverrides`,
> `vehicleOverrides`, `lishiVehicleData`, `lishiInventory`, `customVehicles`,
> `vehicleYearNotes`, `toolsDatabase` — starts **empty** and is filled from
> IndexedDB and Firebase.
>
> Before v245 those literals held a snapshot dated **2026-02-10**, seven months
> stale, and it was not inert: PN B79's keyway was pinned at `B62` and B89's at
> `B86`, overriding live corrections on every load.
>
> **Counts below were measured from that snapshot and are historical.** For
> current figures, read an export (Settings → Export Data), never the literals.
> A live export as of 2026-09-22 holds 862 records, 387 custom keys, 1065
> priced part numbers, 354 FID assignments and 588 distinct FCC IDs.

## 1. Where records live

| Store | Shape | Holds |
|---|---|---|
| `DB` (`index.html:8736`) | array, 515 | Seed catalog, baked into the file. Keyless Ride-sourced. |
| `customKeys` (`index.html:25939`) | array, 41 | Every key you added by hand. Persisted + synced. |
| — | — | `loadCustomKeysIntoDB()` merges `customKeys` into `DB` at boot, so `DB` in memory is *seed + yours*. |
| `customVehicles` | array, 28 | Standalone vehicle records (vehicle-first, not key-first). |
| `lishiVehicleData` | map `make\|model\|startYear\|endYear` → obj, 168 | Per-vehicle key data used by the Vehicle search page. |

**Two write paths, different shapes.** A seed key edited in the app does *not*
get mutated in place — the edit goes to an override store (`keyInfoOverrides`,
`vehicleOverrides`). Only `customKeys` records are edited directly.

---

## 2. KEY — every field

### 2.1 Identity

| Field | Type | Form input | Fill rate | Notes |
|---|---|---|---|---|
| `pn` | string\|number | `customKeyPN` | 515/515 | **Primary key.** Everything else joins on this. Matched case-insensitively. |
| `fccid` | string | `customKeyFCC` | 500/515 | Can hold a comma-joined list (`"NBG009768T, NBGG093UCC"`). For non-chip keys it defaults to the PN. |
| `brand` | string | `customKeyBrand` / `…BrandOther` | **0** | Written by the current form; **absent from all stored records.** See §6. |
| `altParts` | array | `gatherCustomKeyAltParts()` | **0** | Additional PNs for the same key. Same drift. |
| `mergedPNs` | array of string | — | 38/515 | Seed-only. Other PNs folded into this record. |
| `secondaryKeyPartNumber` | — | — | **0/515** | Dead field. Present in the schema, never populated. |
| `memorySeat` | number\|null | `customKeyMemorySeat` / `editKeyInfoMemorySeat` | 2 | Driver memory position (1/2/3) when the fob is paired to a seat/mirror profile. Added v242. |

### 2.2 Physical / spec

| Field | Type | Form input | Fill rate | Notes |
|---|---|---|---|---|
| `keyType` | string | `customKeyType` | 513/515 | 12 fixed options + custom. Drives which form sections show. |
| `keyway` | string | `customKeyKeyway` | 437/515 | Blade profile. Joins to `lishiData`. |
| `chip` | string | `customKeyChip` | 440/515 | Transponder. `"No"` / null = non-chip. |
| `buttons` | string | `customKeyButtons` | 367/515 | Long canonical strings, e.g. `3 Button: L, U, P (Lock, Unlock, Panic)`. ~30 presets. |
| `frequency` | string | `customKeyFreq` | 367/515 | `305-320 MHz`, `434 MHz`, … |
| `battery` | string | `customKeyBatteryType` + `…Qty` | 358/515 | Composed as `"2032 qty 1"` — **not** two fields on the record. |
| `isVATS` | boolean | derived from `keyType` | 33/515 | True when `keyType` is `VATS*`. |
| `removableSleeve` | boolean | `customKeySleeve` | **0** | Form-only drift. |
| `dualKeyRole` | string | — | 12 | `ignition` \| `door`. Seed/legacy. |
| `keyRoles` | array | `customKeyRole{Ignition,Door,Glovebox,Trunk}` | **0** | Current form's replacement for `dualKeyRole`. `KEY_ROLES = ['ignition','door','glovebox','trunk']` (`index.html:22910`). |

### 2.3 Commerce — **read carefully, there are two systems**

| Field | Type | Fill rate | Notes |
|---|---|---|---|
| `priceA` | string\|number | 464/515 | Legacy, on the record. |
| `priceB` | string\|number | 200/515 | Legacy. |
| `emergencyPrice` / `emergencyPN` | | 185 / 226 | Legacy. |
| `shellPrice` / `shellPN` | | 87 / 131 | Legacy. |
| `oem` | number\|string | 31/515 | Legacy. Inconsistent — some values carry `$`. |
| ~~`distributorPrices`~~ | — | **removed v240** | Was a duplicate copy of the Keyless Ride prices. Nothing read it; stripped from all 470 records. |
| `inStock` | string | 279/515 | `Yes` / `No` / `Remote only`. Free text, not a boolean. |
| `stockQty` | number | 470/515 | All zero in seed. Live counts live in `stockData`. |

**The live price store is `vendorPrices`, not the record.**
`initializeVendorPricesFromDB()` seeds it from the record's flat
`priceA`/`priceB`/`emergencyPrice`/`shellPrice`/`oem` **once** (guarded by
`vendorPriceSeedV1`), then `renderVendorPrices()` reads *only* `vendorPrices`.
Those flat fields are therefore **seed-only**: read at first boot, never again.

`priceA`/`priceB` are Keyless Ride's new/refurbished split — KR lists those as
separate `XXXXA`/`XXXXB` part numbers. They are **not** a generic vendor
concept: of the three vendors with prices, only Keyless Ride uses `priceB`
(202 of 610 keys); Keyless-City (19) and American Key Supply (1) use `priceA`
only, where it just means "the price". The UI still labels both slots
`Price A`/`Price B` for every vendor.

> **Importer rule:** write `vendorPrices[pn][vendorName]`, and put a non-KR
> vendor's single price in `priceA`. Values are stored without a leading `$`
> (see `normalizePriceInput`).

### 2.4 Display / meta

| Field | Fill rate | Notes |
|---|---|---|
| `vehicles` | 515/515 | The application list — see §3.1. |
| `vehiclesRaw` | 377/515 | Original catalog text, e.g. `Grand Cherokee (22-23), Wagoneer (22-23)`. Parsed into `vehicles`. |
| `originalMake` | 436/515 | Catalog make grouping, e.g. `Chrys/Dodge/Jeep/Ram`. Not the same as `vehicles[].make`. |
| `logo` | 375/515 | `"Jeep logo"` — display only. |
| `notes` | 99/515 | Free text (`customKeyNotes`). |
| `ilco` | 7/515 | Ilco cross-ref. Almost entirely empty — an import target. |
| `isCustom` | 40/515 | Marks user-added. |
| `addedDate` | 40/515 | ISO timestamp. |

---

## 3. VEHICLE — every field

There are **two different vehicle shapes**. Don't conflate them.

### 3.1 Vehicle application (sub-object inside a key's `vehicles[]`)

1847 entries across all keys.

| Field | Type | Form input | Fill rate | Notes |
|---|---|---|---|---|
| `make` | string | `customVehicleMake{n}` | 1847/1847 | Required. |
| `model` | string | `customVehicleModel{n}` / `…Other{n}` | 1847/1847 | Falls back to `'Unknown'`. |
| `startYear` | number | `customVehicleStart{n}` | 1847/1847 | Defaults to current year. |
| `endYear` | number | `customVehicleEnd{n}` | 1847/1847 | Defaults to `startYear`. |
| `codeSeries` | array\|string\|null | per-row builder | 195/1847 | **Three shapes in live data:** `[{series,years}]`, a bare string (`"Z0001-Z6000"`), and `[]`. Any consumer must handle all three. |
| `startMonth` / `endMonth` | number | `customVehicle{Start,End}Month{n}` | 0 | Collected by the form, only written when non-zero. |
| roles | — | `customVehicleRole{n}_{role}` | 0 | Per-vehicle role flags. |

A single form row can emit **several** entries — the "extra year range"
controls (`customVehicleExtra*`) produce additional objects with the same
make/model but different year spans and `codeSeries: null`.

### 3.2 Standalone vehicle record (`customVehicles`, and `lishiVehicleData`)

These carry key data *per vehicle* rather than per part number, and they
split by lock position with `ignition_` / `door_` prefixes.

| Field | Form input (`vehicleData*`) | customVehicles | lishiVehicleData |
|---|---|---|---|
| `make`, `model`, `startYear`, `endYear` | — | 28 / 28 / 24 / 24 | (encoded in the map key) |
| `id` | — | 28 | — |
| `createdAt`, `lastEdited`, `lastEditedBy` | — | 28 / 20 / — | — / 107 / 107 |
| `dataStatus` | — | 28 (`complete`\|`partial`) | — |
| `multipleKeys` | `vehicleDataMultipleKeys` | 20 | 105 |
| `keyBlank` | `vehicleDataKeyBlank` | 13 | 46 |
| `keyType` | `vehicleDataKeyType` | 24 | 32 |
| `keyway` | `vehicleDataKeyway` | 15 | 74 |
| `fccid` | `vehicleDataFccid` | 15 | 67 |
| `chip` | `vehicleDataChip` | 8 | 63 |
| `frequency` | `vehicleDataFrequency` | 5 | 32 |
| `buttons` | `vehicleDataButtons` | 4 | 2 |
| `battery` | `vehicleDataBattery` | — | 1 |
| `secondaryKeyPN` | `vehicleDataSecondaryKey` | — | 0 |
| `codeSeries` | builder | — | 152 |
| `differentCodeSeries` | `vehicleDataDiffCodeSeries` | — | 31 |
| `notes` | `vehicleDataNotes` | 5 | 4 |
| `ignition_keyBlank` / `_chip` / `_keyway` / `_fccid` / `_buttons` / `_frequency` / `_codeSeries` | `vehicleDataIgn*` | 9 each (buttons/freq 0) | 31 each (buttons/freq 0) |
| `door_keyBlank` / `_chip` / `_keyway` / `_fccid` / `_codeSeries` | `vehicleDataDoor*` | 9 each (chip 0) | 31 each (chip 3) |
| `yearOverrides`, `overrideNotes`, `hasOverrides` | — | 24 each | — |
| `confirmCount`, `lastConfirmed`, `lastConfirmedBy` | — | — | 168 / 1 / 0 |
| `direction` | — | — | 2 (`CW`\|`CCW`) |
| `ilcoPartNum`, `keystonePartNum` | — | **0** | — | 

`ilcoPartNum` and `keystonePartNum` exist in the vehicle schema and are
completely empty — the most obvious import targets on the vehicle side.

Saving a key also writes its vehicles into `lishiVehicleData` (end of
`saveNewCustomKey`), so the two stores are kept loosely in sync.

---

## 4. Satellite stores

All 32 IndexedDB stores, with the ones joined to keys/vehicles called out.

**Keyed by part number (`pn`):**

| Store | Value shape | Live count |
|---|---|---|
| `vendorPrices` | `{vendor: {priceA, priceB, oem, emergency, shell}}` | the real price store |
| `stockData` | number | per-user, **excluded from shared sync** |
| `keyInfoOverrides` | partial key record — edits to seed keys | 1 |
| `vehicleOverrides` | `vehicles[]` — vehicle edits to seed keys | — |
| `keyRelationships` | `{keyway, typicalChip}` | 604 |
| `pnSupersessions` | `oldPN → newPN` | 0 | means **renamed** — `pruneSupersededCustomKeys()` deletes a custom key whose PN appears here |
| `discontinuedPns` | `{replacedBy, date, note}` | 0 | means **discontinued** — record, stock and history all survive. Added v243 |
| `pnRenames` | `oldPN → newPN` | 0 |
| `autelIKeyCompatibility` | `[tags]` | 0 |

**Keyed by FCC ID:**

| Store | Value shape | Live count |
|---|---|---|
| `fccRelationships` | `{frequency, buttonCount}` | 186 |
| `fidAssignments` | `fccid → FID` (e.g. `"H73" → "F-102"`) | many |
| `fidPrefixes` | `prefix → {makes[], name}` | `N`, `F`, `GM`, … |
| `fidMerges` | — | 0 |

**Keyed by keyway:** `lishiData` → `{tool, direction}`; `customKeyways`.

**Keyed by `make\|model`:** `vehicleYearNotes`.

**Other:** `purchaseHistory`, `jobHistory`, `activeJobs`, `toolsDatabase`,
`lishiInventory`, `testKeyInventory`, `customVendors`, `customDropdownValues`,
`ilcoRef`, `ilcoReviewQueue`, `antiqueCustom`, `antiqueOverrides`,
`deletedLishiTools`, `deletedPnSupersessions`.

---

## 5. Relationships

```
                        ┌──────────────┐
                        │  KEY  (pn)   │
                        └──────┬───────┘
      ┌────────────┬───────────┼───────────┬──────────────┐
      │            │           │           │              │
  vendorPrices  stockData  keyInfo…   keyRelationships  vehicles[]
   [pn][vendor]    [pn]    Overrides[pn]    [pn]            │
      │                                                     │
      │                              ┌──────────────────────┴───┐
      │                              │ make, model, start/endYear│
      │                              │ codeSeries               │
      │                              └──────────┬───────────────┘
      │                                         │ make|model|start|end
      ▼                                         ▼
  DEFAULT_VENDORS + customVendors        lishiVehicleData
  (8 vendors)                            customVehicles
                                         vehicleYearNotes (make|model)

   KEY.fccid ──► fccRelationships[fccid] ──► {frequency, buttonCount}
             └─► fidAssignments[fccid] ──► FID ──► fidPrefixes[prefix]

   KEY.keyway ──► lishiData[keyway] ──► {tool, direction}
```

**Vendors** (`DEFAULT_VENDORS`, `index.html:8739`): Keyless Ride, Banner
Solutions, Hans Johnsen, IML, Keyless-City (`distributor`); UHS Hardware,
American Key Supply, Locksmith Keyless (`online`). Plus `customVendors`.

---

## 5b. Normalizing imported data

Incoming distributor data does not have to match the app's vocabulary — some
of it is normalized on the way in, so the extractor should copy sources
verbatim rather than guess at our formats.

| field | normalizer | behaviour |
|---|---|---|
| `frequency` | `bucketizeFrequency()` | Parses the digits and buckets by average, so `315MHz`, `315 MHz` and `314.95 MHz` all become `305-320 MHz`; `433 Mhz`, `434MHz` and `433.66-434.18 MHz` all become `434 MHz`. Spacing and case are irrelevant. A value outside every known band (e.g. `13.56 MHz`) is left untouched, as is text with no digits (`None (Transponder Only)`). |
| price fields | `normalizePriceInput()` | Strips a leading `$` and thousands commas; leaves non-numeric notes as typed. |
| `battery` | — | Canonical form is `<type> qty <n>`, e.g. `2032 qty 1`. Three spellings exist in legacy data (`2032`, `2032 x1`, `2032 qty 1`); only the last is written by the form. |
| `chip` | **none yet** | 48 distinct values, already inconsistent in case. Distributors use a different vocabulary again (`ID46`, `ID47`, `4A` vs our `PH 46`, `Hitag AES PCF7953M`). **A mapping table is still needed before any import can fill this field.** |

Bands, for reference: 300–400 → `305-320 MHz`, 420–450 → `434 MHz`,
850–885 → `868 MHz`, 890–915 → `902 MHz`, 915–935 → `923 MHz`.

## 5c. Condition variants

One key by FCC ID is often several things to buy. Keyless Ride splits new and
refurbished into separate part numbers `XXXXA` / `XXXXB`; Your Car Key Guys
sells the same key as `OEM Brand New` ($85) and `OEM Board OEM Shell` ($45).
Same FCC ID, same OEM part number, different SKU and price.

The existing `priceA` / `priceB` slots in `vendorPrices[pn][vendor]` are that
idea, hard-coded to one distributor's vocabulary. Extraction now carries it
generically as `variants[{ vendorSku, price, condition, label }]` with
`condition` in `new | reclaimed | refurbished | aftermarket | shell-only |
unknown`, so the importer can map a condition onto the right price slot rather
than guessing from a shop's wording.

**Resolved by batch 3: two price slots are not enough.** Across 20 Nissan
products one shop listed 60 variants - 7 products with 4 and one with 5, using
four conditions (reclaimed 23, new 14, refurbished 13, aftermarket 10) and
price spreads as wide as $21.50 to $125 on a single key. Each variant carries
its own SKU, not the product's repeated.

So `vendorPrices[pn][vendor]` needs a variants array rather than the
`priceA`/`priceB` pair. The existing two slots stay as the Keyless Ride data
already stored, mapping `new -> priceA` and the cheapest reclaimed or
refurbished `-> priceB` for backward compatibility.

## 5d. Chip names are inconsistent at the FCC level

Extraction across 40 Your Car Key Guys products shows one FCC ID carrying
different transponder names on different listings from the same shop:

| FCC ID | chip names seen |
|---|---|
| `KR5S180144014` | `ID47` (3 products), `4A` (4), `ID46` (1) |
| `KR55WK49622` | `ID46` (2), `ID47` (1) |

So the chip funnel cannot be a plain string mapping. Deciding `ID47 -> PH 47`
would still leave the same FCC ID holding three different answers. The funnel
has to resolve per FCC ID and flag a disagreement for review, not per raw
string.

## 6. Schema drift — fix before importing

Four fields are written by the **current** Add Custom Key form but appear in
**zero** stored records, meaning every key predates them:

- `brand` — and `purchaseHistory` entries already reference it
- `altParts` — additional part numbers
- `removableSleeve`
- `keyRoles` — supersedes the older `dualKeyRole` (12 records still use the old one)

Two more are schema-present and never populated: `secondaryKeyPartNumber`
(keys), `ilcoPartNum` / `keystonePartNum` (vehicles).

Other traps for an importer:

1. **`codeSeries` has three shapes** — array of `{series, years}`, bare string, or `[]`.
2. **`battery` is composed** (`"2032 qty 1"`), not structured.
2b. **Prices are stored bare** — no `$`, no thousands commas. `normalizePriceInput()`
   enforces this on write; `priceRow()` adds the `$` at render time and leaves
   non-numeric entries (e.g. `Discontinued`) unprefixed.
3. **`inStock` is free text**, not a boolean; `stockQty` is separate and always 0 in seed.
4. **`fccid` may be a comma-joined list.**
5. **`oem` price sometimes carries a `$`.**
6. **`originalMake` ≠ `vehicles[].make`** — the first is a catalog grouping.
7. **Seed keys can't be mutated** — edits must go to `keyInfoOverrides` / `vehicleOverrides`.
8. **Every new store must be wired in 6 places** (per `CLAUDE.md`): a `saveX()`,
   `loadLocalData()`, both `uploadToCloud()` payloads, the `downloadFromCloud()`
   merge, and `exportData()`/`importData()`.
